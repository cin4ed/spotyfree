name: Build and deploy frontend

on:
  push:
    paths:
      - frontend/**

jobs:
  Build-Svelte-App:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: 🥬 Setup NodeJS
        uses: actions/setup-node@v1
        with:
          node-version: 16

      - name: 📦 Install dependencies
        run: cd ${{ github.workspace }} && npm i

      - name: 🛠 Build app
        run: npm run build

      - name: 📲 Send files to linode server
        run: ls